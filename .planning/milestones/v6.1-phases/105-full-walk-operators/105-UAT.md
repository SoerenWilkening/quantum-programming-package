---
status: complete
phase: 105-full-walk-operators
source: [105-01-SUMMARY.md, 105-02-SUMMARY.md]
started: 2026-03-05T14:30:00Z
updated: 2026-03-05T14:35:00Z
---

## Current Test

[testing complete]

## Tests

### 1. r_a() Even-Depth Walk Operator
expected: Running r_a() on a chess position produces a QuantumCircuit that applies diffusion at even depths (excluding root and leaves). For max_depth=3, r_a covers depth 2 only.
result: pass

### 2. r_b() Odd-Depth + Root Walk Operator
expected: Running r_b() on a chess position produces a QuantumCircuit that applies diffusion at odd depths plus root. For max_depth=3, r_b covers depths 1 and 3 (odd=1, root=3).
result: pass

### 3. R_A/R_B Depth Disjointness and Completeness
expected: The depth sets used by r_a and r_b are disjoint (no overlap) and their union covers all depths 1..max_depth. Depth 0 (leaves) is excluded from both.
result: pass

### 4. all_walk_qubits() Mega-Register
expected: all_walk_qubits() returns a qint wrapping height + branch register qubits. The qint width equals the sum of height register bits and branch register bits (not including board qubits due to 64-qubit limit).
result: pass

### 5. walk_step() Compiled Walk Operator
expected: walk_step() produces a compiled function that composes R_A then R_B (U = R_B * R_A). Calling it on a chess position returns a valid circuit. Repeated calls use caching (same compiled function reused).
result: pass

### 6. All Tests Pass (No Regressions)
expected: Running `pytest tests/python/ -v` completes with all tests passing and zero failures.
result: skipped
reason: Pre-existing failure in test_api_coverage.py::TestQintAPI::test_qint_default_width (qint auto-sizes to min bits instead of defaulting to 8). Unrelated to Phase 105.

## Summary

total: 6
passed: 5
issues: 0
pending: 0
skipped: 1

## Gaps

[none yet]
