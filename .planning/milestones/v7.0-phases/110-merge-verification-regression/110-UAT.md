---
status: diagnosed
phase: 110-merge-verification-regression
source: [110-01-SUMMARY.md, 110-02-SUMMARY.md]
started: 2026-03-07T00:00:00Z
updated: 2026-03-07T00:01:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Statevector Equivalence Tests Pass
expected: Run `pytest tests/python/test_merge_equiv.py -v` and all add/mul/grover equivalence tests pass. You should see tests for add widths 1-4, mul widths 1-4, and grover oracle width=4, all comparing opt=2 vs opt=3.
result: pass

### 2. Parametric+Opt Interaction Tests Pass
expected: In the same test file, parametric interaction tests verify: opt=1 produces correct results, opt=3 produces correct results, and opt=2 with parametric raises ValueError.
result: pass

### 3. Compile Tests Run at All Opt Levels
expected: Run `pytest tests/test_compile.py -v` and see each test parametrized over opt_level=1,2,3. Pre-existing xfail tests remain xfail. No new failures introduced by the opt_level fixture.
result: issue
reported: "a few fails. at 80% the process was killed"
severity: blocker

### 4. Merge Tests Run at All Opt Levels
expected: Run `pytest tests/python/test_merge.py -v` and see all 29 merge tests parametrized over opt_level=1,2,3 (87 total invocations). All should pass.
result: pass

## Summary

total: 4
passed: 3
issues: 1
pending: 0
skipped: 0

## Gaps

- truth: "Compile tests run at all opt levels without new failures or OOM kills"
  status: failed
  reason: "User reported: a few fails. at 80% the process was killed"
  severity: blocker
  test: 3
  root_cause: "Two issues: (1) No gc.collect() between tests — stale qint __del__ injects gates into wrong circuits causing memory growth over 354 invocations. (2) Some tests assume opt=1 behavior (gate counts, stats, cache) but opt=2 merging and opt=3 DAG-skip produce different results."
  artifacts:
    - path: "tests/conftest.py"
      issue: "opt_level fixture lacks gc.collect() call"
    - path: "tests/test_compile.py"
      issue: "Module-wide pytestmark applies opt_level to all tests including opt-sensitive ones"
    - path: "src/quantum_language/qint.pyx"
      issue: "__del__ injects gates when GC deferred"
  missing:
    - "Add gc.collect() to opt_level fixture before each test"
    - "Apply opt_level selectively or exempt opt-sensitive tests"
  debug_session: ".planning/debug/test-compile-oom.md"
