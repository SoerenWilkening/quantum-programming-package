---
phase: 56-forward-inverse-depth-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_compile.py
autonomous: true

must_haves:
  truths:
    - "Diagnostic test measures depth of f(x) capture vs f(x) replay vs f.adjoint(x) replay"
    - "Test output shows exact depth values for each path"
    - "Root cause of depth discrepancy is identified and documented"
  artifacts:
    - path: "tests/test_compile.py"
      provides: "Diagnostic test functions for depth analysis"
      contains: "test_depth_capture_vs_replay"
  key_links:
    - from: "tests/test_compile.py"
      to: "quantum_language._core.get_current_layer"
      via: "layer measurement"
      pattern: "get_current_layer"
---

<objective>
Diagnose the depth discrepancy between forward compilation (capture), forward replay, and adjoint replay paths.

Purpose: Identify exactly WHERE and WHY forward path produces different circuit depth than inverse path.
Output: Diagnostic tests that expose the discrepancy, documented root cause analysis in test file comments.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-forward-inverse-depth-fix/56-RESEARCH.md
@src/quantum_language/compile.py
@c_backend/src/optimizer.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Diagnostic Depth Comparison Tests</name>
  <files>tests/test_compile.py</files>
  <action>
Add a new test section at the end of tests/test_compile.py with header comment:
```python
# ============================================================================
# DEPTH-DIAG: Forward/Inverse Depth Discrepancy Diagnostic (Phase 56)
# ============================================================================
```

Create the following diagnostic tests:

**Test 1: test_depth_capture_vs_replay**
- Create a simple @ql.compile function that does `x += 1; return x` with width=8
- Measure depth for:
  1. First call (capture path): record start_layer before, end_layer after
  2. Second call on fresh qubits (replay path): record start_layer before, end_layer after
- Print both depths and the difference
- This test SHOULD PASS (just gathers data) - use `print()` for diagnostics

**Test 2: test_depth_forward_vs_adjoint**
- Create a simple @ql.compile function that does `x += 1; return x` with width=8
- Measure depth for:
  1. f(x) on fresh qubits (trigger capture, then measure replay depth)
  2. f.adjoint(x) on fresh qubits (measure adjoint replay depth)
- Print both depths
- Assert they are equal (this is the core issue - may fail initially)

**Test 3: test_depth_across_widths**
- Test the depth comparison across widths 4, 8, 16
- For each width, measure forward replay depth vs adjoint replay depth
- Print a table showing width, forward_depth, adjoint_depth, difference
- Assert all differences are 0

**Test 4: test_layer_floor_effect**
- Add diagnostic prints showing layer_floor values at key points
- Use `from quantum_language._core import _get_layer_floor, get_current_layer`
- Print layer_floor and current_layer before/after capture, before/after replay
- This test just gathers data (no assertion) to understand layer_floor behavior

Import statement needed at top of test section:
```python
from quantum_language._core import get_current_layer, _get_layer_floor
```
  </action>
  <verify>
Run: `pytest tests/test_compile.py -v -k "depth" --tb=short 2>&1 | head -100`
Expected: Tests run and print diagnostic output. Some may fail (exposing the bug).
  </verify>
  <done>
Diagnostic tests exist in tests/test_compile.py. Running them shows depth values for capture, replay, and adjoint paths. If discrepancy exists, test_depth_forward_vs_adjoint fails with clear depth difference.
  </done>
</task>

<task type="auto">
  <name>Task 2: Profile and Document Root Cause</name>
  <files>tests/test_compile.py</files>
  <action>
After running the diagnostic tests, analyze the output to identify the root cause.

Based on the research hypothesis (layer_floor preventing gate reordering during replay), focus on:

1. **Compare capture vs replay layer assignment:**
   - During capture, layer_floor is whatever the circuit's current state is
   - During replay, `_set_layer_floor(start_layer)` forces gates to start at current layer
   - This means replay cannot pack gates into earlier layers even if they could fit

2. **Document findings in test file:**
   Add a docstring to the test section explaining:
   - What the root cause is (based on test output)
   - Where in the code the discrepancy originates (compile.py line numbers)
   - What the expected fix approach is

3. **Create a specific reproduction test:**
   If the root cause is confirmed as layer_floor, create `test_layer_floor_causes_depth_inflation`:
   - Show that without layer_floor constraint, gates pack more efficiently
   - This proves the hypothesis and guides the fix

Format findings as:
```python
# ROOT CAUSE ANALYSIS (Phase 56 FIX-01)
# =====================================
# Finding: [describe what was found]
# Location: compile.py lines [X-Y]
# Mechanism: [how it causes discrepancy]
# Fix approach: [what needs to change]
```
  </action>
  <verify>
Run: `pytest tests/test_compile.py -v -k "depth" -s 2>&1 | tail -50`
Expected: Diagnostic output shows depth values. Root cause documented in test file comments.
  </verify>
  <done>
Root cause is documented in tests/test_compile.py with specific line numbers from compile.py. The mechanism causing depth discrepancy is clearly explained.
  </done>
</task>

</tasks>

<verification>
All checks must pass:
- [ ] Diagnostic tests exist in tests/test_compile.py
- [ ] Tests run without syntax errors
- [ ] Output shows depth measurements for capture, replay, adjoint paths
- [ ] Root cause is documented with specific code locations
- [ ] `pytest tests/test_compile.py -v -k "depth"` executes
</verification>

<success_criteria>
1. Diagnostic tests measure depth of f(x) capture vs f(x) replay vs f.adjoint(x) replay
2. Root cause of depth discrepancy is identified and documented
3. Test output provides clear data for the fix implementation in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/56-forward-inverse-depth-fix/56-01-SUMMARY.md`
</output>
