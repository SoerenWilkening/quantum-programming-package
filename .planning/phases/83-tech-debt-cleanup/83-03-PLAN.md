---
phase: 83-tech-debt-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - build_preprocessor.py
autonomous: true
requirements: [DEBT-02]
gap_closure: true

must_haves:
  truths:
    - "Editing a .pxi file and running the pre-commit hook detects qint_preprocessed.pyx drift and fails the check (exits non-zero)"
    - "The hook still auto-fixes and stages the regenerated file before failing"
    - "When no drift exists, the hook exits 0 and the commit proceeds normally"
  artifacts:
    - path: "build_preprocessor.py"
      provides: "sync_and_stage returns 1 on drift detection"
      contains: "sync_and_stage"
  key_links:
    - from: ".pre-commit-config.yaml"
      to: "build_preprocessor.py"
      via: "entry: python3 build_preprocessor.py --sync-and-stage"
      pattern: "build_preprocessor.py.*sync"
---

<objective>
Fix the pre-commit hook to fail (exit 1) when preprocessor drift is detected, while still auto-fixing and staging the regenerated file.

Purpose: Close SC2 gap — the hook currently auto-fixes but returns 0 (never blocks). Standard pre-commit convention (like black, prettier) is to fix the file AND return non-zero so the developer knows changes were made and re-commits. This satisfies both the user's auto-fix decision and the "fails the check" success criterion.
Output: Updated build_preprocessor.py where sync_and_stage() returns 1 when drift was detected.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-tech-debt-cleanup/83-VERIFICATION.md
@.planning/phases/83-tech-debt-cleanup/83-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make sync_and_stage return non-zero on drift detection</name>
  <files>build_preprocessor.py</files>
  <action>
    In build_preprocessor.py, modify the `sync_and_stage()` function (starting at line 122):

    1. Add a `drift_found = False` flag at the top of the function body.
    2. Inside the `if old_content != new_content:` block (line 146), set `drift_found = True`.
    3. Change the return statement from `return 0` to `return 1 if drift_found else 0`.
    4. Update the docstring to reflect the new behavior:
       ```
       Returns:
           0 if all preprocessed files are current (no drift).
           1 if drift was detected and auto-fixed (commit should be retried).
       ```

    This follows the standard pre-commit auto-fix pattern: fix the files AND return non-zero so the developer sees changes were staged and re-commits. The .pre-commit-config.yaml entry does NOT need changes — it already invokes `--sync-and-stage` which will now correctly exit 1 on drift.

    Verify:
    - Corrupt qint_preprocessed.pyx (append a comment), then run `python3 build_preprocessor.py --sync-and-stage; echo "exit: $?"` — should print "Drift detected" and exit 1.
    - Run again immediately (no drift) — should exit 0.
    - Run `pre-commit run sync-preprocessed-pyx --all-files` on a clean state — should pass (exit 0).
  </action>
  <verify>
    1. `python3 -c "
import build_preprocessor, inspect
src = inspect.getsource(build_preprocessor.sync_and_stage)
assert 'drift_found' in src, 'drift tracking missing'
assert 'return 1' in src or 'drift_found' in src, 'non-zero return missing'
print('OK: sync_and_stage tracks drift and returns non-zero')
"`
    2. Corrupt qint_preprocessed.pyx: `echo '# test drift' >> src/quantum_language/qint_preprocessed.pyx && python3 build_preprocessor.py --sync-and-stage; echo "exit: $?"` — shows "Drift detected" and "exit: 1"
    3. Run again clean: `python3 build_preprocessor.py --sync-and-stage; echo "exit: $?"` — shows "exit: 0"
  </verify>
  <done>sync_and_stage() returns 1 when drift is detected (after auto-fixing and staging), returns 0 when no drift. The pre-commit hook now satisfies SC2: "detects drift and fails the check."</done>
</task>

</tasks>

<verification>
1. `python3 build_preprocessor.py --sync-and-stage` exits 0 when preprocessed file is current
2. After corrupting qint_preprocessed.pyx, `python3 build_preprocessor.py --sync-and-stage` exits 1 and regenerates the file
3. `python3 build_preprocessor.py --check` still works independently (exits 1 on stale, 0 on current)
4. Pre-commit hook invocation pattern unchanged (.pre-commit-config.yaml not modified)
</verification>

<success_criteria>
- sync_and_stage() returns 1 when drift detected, 0 when clean
- Auto-fix behavior preserved (regenerate + git add)
- SC2 satisfied: hook "fails the check" on drift
- No changes to .pre-commit-config.yaml needed
</success_criteria>

<output>
After completion, create `.planning/phases/83-tech-debt-cleanup/83-03-SUMMARY.md`
</output>
