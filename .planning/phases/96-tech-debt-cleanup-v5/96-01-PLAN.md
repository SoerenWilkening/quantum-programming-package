---
phase: 96-tech-debt-cleanup-v5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/quantum_language/_core.pxd
  - src/quantum_language/qint.pyx
autonomous: true
requirements: []

must_haves:
  truths:
    - "toffoli_mod_reduce and toffoli_cmod_reduce declarations are absent from _core.pxd"
    - "toffoli_cdivmod_cq and toffoli_cdivmod_qq imports are absent from qint.pyx"
    - "Cython extensions compile cleanly after removal"
    - "All existing tests pass with zero regressions"
  artifacts:
    - path: "src/quantum_language/_core.pxd"
      provides: "Cython extern declarations for C backend"
      contains: "toffoli_mod_add_cq"
    - path: "src/quantum_language/qint.pyx"
      provides: "qint class with division dispatch"
      contains: "toffoli_divmod_cq, toffoli_divmod_qq"
  key_links:
    - from: "src/quantum_language/qint.pyx"
      to: "src/quantum_language/_core.pxd"
      via: "cimport"
      pattern: "from ._core cimport"
---

<objective>
Remove dead Cython declarations and unused imports identified by the v5.0 milestone audit.

Purpose: Eliminate dead code that could mislead future development — `toffoli_mod_reduce`/`toffoli_cmod_reduce` declarations in `_core.pxd` are unreachable since Phase 92 replaced them with Beauregard primitives; `toffoli_cdivmod_cq`/`toffoli_cdivmod_qq` imports in `qint.pyx` are imported but never called.
Output: Clean `_core.pxd` and `qint.pyx` with no dead declarations or imports.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-tech-debt-cleanup-v5/96-RESEARCH.md

Key source files to modify:
@src/quantum_language/_core.pxd
@src/quantum_language/qint.pyx

<interfaces>
<!-- Current state of dead code in _core.pxd (lines 74-78): -->
```cython
	# Toffoli Modular Reduction (Phase 91)
	void toffoli_mod_reduce(circuit_t *circ, const unsigned int *value_qubits,
	                        int value_bits, int64_t modulus)
	void toffoli_cmod_reduce(circuit_t *circ, const unsigned int *value_qubits,
	                         int value_bits, int64_t modulus, unsigned int ext_ctrl)
```

<!-- Current state of dead imports in qint.pyx (lines 31-32): -->
```cython
    toffoli_divmod_cq, toffoli_divmod_qq,
    toffoli_cdivmod_cq, toffoli_cdivmod_qq,
```

<!-- The block starting at line 80 (Toffoli Modular CQ Addition) immediately follows and should remain. -->
<!-- The toffoli_divmod_cq, toffoli_divmod_qq import on line 31 IS used and must remain. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead declarations from _core.pxd and dead imports from qint.pyx</name>
  <files>src/quantum_language/_core.pxd, src/quantum_language/qint.pyx</files>
  <action>
1. Open `src/quantum_language/_core.pxd` and remove the entire "Toffoli Modular Reduction (Phase 91)" block (lines 74-78). This is 5 lines: the comment line, plus the two function declarations (`toffoli_mod_reduce` and `toffoli_cmod_reduce`) with their parameters. After removal, the "Toffoli Modular CQ Addition/Subtraction (Phase 92)" block (currently at line 80) should follow directly after the `toffoli_cdivmod_qq` declaration block ending at line 72.

2. Open `src/quantum_language/qint.pyx` and remove `toffoli_cdivmod_cq, toffoli_cdivmod_qq,` from the cimport block (line 32). The preceding line (`toffoli_divmod_cq, toffoli_divmod_qq,` on line 31) MUST remain — those functions ARE actively used for division dispatch. After removal, the closing `)` on the former line 33 should follow directly after the `toffoli_divmod_cq, toffoli_divmod_qq,` line.

IMPORTANT: Do NOT touch any C source files (`ToffoliModReduce.c`, `ToffoliDivision.c`) or C header files (`toffoli_arithmetic_ops.h`). Only the Cython `.pxd` and `.pyx` files have dead references. The C code remains valid and may be useful for future phases.

Per user decision: clean removal, no stubs. If needed later, recover from git history.
  </action>
  <verify>
    <automated>cd /Users/sorenwilkening/Desktop/Projects/Quantum_Assembly && python setup.py build_ext --inplace 2>&1 | tail -5</automated>
  </verify>
  <done>
- `_core.pxd` no longer contains `toffoli_mod_reduce` or `toffoli_cmod_reduce`
- `qint.pyx` no longer contains `toffoli_cdivmod_cq` or `toffoli_cdivmod_qq`
- Cython extensions build successfully
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite to verify zero regressions</name>
  <files></files>
  <action>
Run the full Python test suite to verify that removing dead declarations and imports causes no regressions. All tests that passed before must still pass.

```bash
pytest tests/python/ -v --tb=short 2>&1 | tail -30
```

If any test fails, investigate whether the failure is caused by the removal (it should not be, since the removed declarations/imports were unused). If a pre-existing failure is found, document it but do not fix it (out of scope).
  </action>
  <verify>
    <automated>cd /Users/sorenwilkening/Desktop/Projects/Quantum_Assembly && pytest tests/python/ -v --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>
- All tests pass (same pass count as before removal)
- No new failures introduced by dead code removal
  </done>
</task>

</tasks>

<verification>
1. `grep -r 'toffoli_mod_reduce' src/quantum_language/_core.pxd` returns empty (declaration removed)
2. `grep -r 'toffoli_cmod_reduce' src/quantum_language/_core.pxd` returns empty (declaration removed)
3. `grep -r 'toffoli_cdivmod_cq' src/quantum_language/qint.pyx` returns empty (import removed)
4. `grep -r 'toffoli_cdivmod_qq' src/quantum_language/qint.pyx` returns empty (import removed)
5. `grep -r 'toffoli_divmod_cq' src/quantum_language/qint.pyx` returns a match (used import preserved)
6. Cython build completes without errors
7. `pytest tests/python/ -v` shows all tests passing
</verification>

<success_criteria>
- Phase 96 SC1: Dead `toffoli_mod_reduce` declaration removed from `_core.pxd`
- Phase 96 SC2: Dead `toffoli_cdivmod_cq/qq` imports removed
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/96-tech-debt-cleanup-v5/96-01-SUMMARY.md`
</output>
