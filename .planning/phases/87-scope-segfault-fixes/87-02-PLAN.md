---
phase: 87-scope-segfault-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - BUG-09

must_haves:
  truths:
    - "BUG-09 (BUG-MOD-REDUCE) is explicitly marked as deferred in REQUIREMENTS.md"
    - "The deferral rationale explains why a simple fix is insufficient"
    - "The Beauregard-style redesign requirement is documented"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "BUG-09 marked as explicitly deferred with rationale"
      contains: "Deferred"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/86-qft-bug-fixes/86-03-SUMMARY.md"
      via: "References Phase 86-03 uncomputation architecture findings"
      pattern: "uncomputation"
---

<objective>
Document BUG-09 (BUG-MOD-REDUCE) deferral with rationale explaining why modular reduction needs a Beauregard-style algorithm redesign.

Purpose: User decision to explicitly defer BUG-09 rather than attempt a fix. The `_reduce_mod` method has a fundamental architecture limitation: iterative comparisons accumulate un-uncomputed temporaries that corrupt the remainder register. This requires algorithmic redesign, not a patch.
Output: Updated REQUIREMENTS.md with deferral documentation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-scope-segfault-fixes/87-RESEARCH.md
@.planning/phases/86-qft-bug-fixes/86-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document BUG-09 deferral in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
1. Read `.planning/REQUIREMENTS.md` and locate the BUG-09 entry in the Bug Fixes section.

2. Update BUG-09 status from `[ ]` to `[-]` (deferred) and add "Explicitly deferred" annotation:
   ```
   - [-] **BUG-09**: Fix _reduce_mod result corruption (BUG-MOD-REDUCE) — **Explicitly deferred**: requires Beauregard-style algorithm redesign (see rationale below)
   ```

3. In the Traceability table, update BUG-09's status from "Pending" to "Explicitly deferred" and its Phase from "Phase 87" to "Future (Beauregard redesign)".

4. In the "Future Requirements" section, add a detailed deferral rationale under "Uncomputation Architecture Redesign" (which already lists BUG-06 and BUG-08). Add BUG-09 with this rationale:
   - Root cause: `_reduce_mod` uses iterative `>=` comparison and conditional subtraction. Each comparison allocates temporary widened qints that entangle with the value register. Across multiple iterations, un-uncomputed comparison qbools accumulate entanglement, corrupting the remainder. (Phase 38 research, Phase 86-03 findings.)
   - Why a simple fix is insufficient: The uncomputation architecture cannot handle orphan temporaries from comparison operators (`operation_type=None`, `creation_scope=0`, `_start_layer=_end_layer=0`). These temporaries are invisible to both cascade uncomputation and lazy GC cleanup.
   - Required approach: Beauregard-style modular arithmetic (2003) which uses a fundamentally different circuit topology: `(a+b) mod N` implemented as add(a,b), sub(N), add(N) controlled by sign bit, sub(b) controlled by sign bit. This requires careful ancilla management and a purpose-built circuit structure.
   - No specific future phase assigned.

5. Verify the file is well-formed markdown.
  </action>
  <verify>
    <automated>cd /Users/sorenwilkening/Desktop/Projects/Quantum_Assembly && grep -c "BUG-09" .planning/REQUIREMENTS.md</automated>
    <manual>BUG-09 shows as explicitly deferred with Beauregard redesign rationale</manual>
  </verify>
  <done>BUG-09 is marked as explicitly deferred in REQUIREMENTS.md with full rationale referencing Phase 38 research, Phase 86-03 findings, and the Beauregard-style redesign requirement. Traceability table updated.</done>
</task>

</tasks>

<verification>
- `grep "BUG-09" .planning/REQUIREMENTS.md` shows "Explicitly deferred"
- Deferral rationale mentions Beauregard, Phase 38, and Phase 86-03
- Traceability table shows BUG-09 as "Explicitly deferred" under "Future (Beauregard redesign)"
</verification>

<success_criteria>
- BUG-09 marked as explicitly deferred (not pending, not completed)
- Rationale explains why `_reduce_mod` cannot be patched
- Beauregard-style redesign requirement is documented
- No code changes needed for this plan
</success_criteria>

<output>
After completion, create `.planning/phases/87-scope-segfault-fixes/87-02-SUMMARY.md`
</output>
