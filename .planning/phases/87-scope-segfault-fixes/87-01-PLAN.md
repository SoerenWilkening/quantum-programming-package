---
phase: 87-scope-segfault-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - c_backend/include/types.h
  - tests/python/test_bug01_32bit_mul.py
autonomous: true
requirements:
  - BUG-01

must_haves:
  truths:
    - "32-bit multiplication completes without segfault and produces a valid circuit"
    - "64-bit multiplication has sufficient headroom in MAXLAYERINSEQUENCE"
    - "Small widths (1-16 bit) continue to work correctly with no regression"
  artifacts:
    - path: "c_backend/include/types.h"
      provides: "MAXLAYERINSEQUENCE constant set to 300000"
      contains: "MAXLAYERINSEQUENCE 300000"
    - path: "tests/python/test_bug01_32bit_mul.py"
      provides: "Regression test for 32-bit multiplication segfault"
      min_lines: 15
  key_links:
    - from: "c_backend/include/types.h"
      to: "c_backend/src/IntegerMultiplication.c"
      via: "MAXLAYERINSEQUENCE used in calloc for sequence layer allocation"
      pattern: "MAXLAYERINSEQUENCE"
---

<objective>
Fix 32-bit multiplication segfault by increasing MAXLAYERINSEQUENCE constant from 10,000 to 300,000 to accommodate up to 64-bit QQ_mul (276,766 layers).

Purpose: 32-bit multiplication currently segfaults because QQ_mul(32) requires 36,494 layers but the buffer only holds 10,000. This is the simplest fix in Phase 87 -- a single constant change.
Output: Working 32-bit multiplication circuit generation, regression test.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-scope-segfault-fixes/87-RESEARCH.md
@c_backend/include/types.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase MAXLAYERINSEQUENCE and add regression test</name>
  <files>c_backend/include/types.h, tests/python/test_bug01_32bit_mul.py</files>
  <action>
1. In `c_backend/include/types.h`, change `#define MAXLAYERINSEQUENCE 10000` to `#define MAXLAYERINSEQUENCE 300000`. This provides headroom for 64-bit QQ_mul (276,766 layers with ~8% margin). Per user decision: static increase only, no dynamic calculation.

2. Rebuild the project: `pip install -e .` (to recompile C backend with new constant).

3. Create `tests/python/test_bug01_32bit_mul.py` with a regression test:
   - Test that 32-bit QQ_mul generates a circuit without segfault (NO simulation -- 32*3=96 qubits exceeds 17-qubit limit)
   - Create two 32-bit qints, multiply them, export to OpenQASM, assert non-empty output
   - Add a smaller width test (e.g., 8-bit) that verifies multiplication still produces correct results via Qiskit simulation (8*3=24 qubits -- too large for sim, so use width 4 or 5 for sim verification, width 8 for generation-only)
   - For simulation verification: use width 4 (4*3=12 qubits), multiply known values, verify result via Qiskit statevector

4. Run the new test: `pytest tests/python/test_bug01_32bit_mul.py -v`
  </action>
  <verify>
    <automated>cd /Users/sorenwilkening/Desktop/Projects/Quantum_Assembly && pytest tests/python/test_bug01_32bit_mul.py -v</automated>
    <manual>Confirm 32-bit multiplication no longer segfaults</manual>
  </verify>
  <done>32-bit QQ_mul generates a valid circuit without segfault. MAXLAYERINSEQUENCE is 300,000 supporting up to 64-bit. Regression test committed.</done>
</task>

</tasks>

<verification>
- `grep -n "MAXLAYERINSEQUENCE" c_backend/include/types.h` shows 300000
- `pytest tests/python/test_bug01_32bit_mul.py -v` passes
- No segfault on 32-bit multiplication
</verification>

<success_criteria>
- MAXLAYERINSEQUENCE changed from 10000 to 300000 in types.h
- 32-bit multiplication circuit generates without crash
- Regression test exists and passes
- Small-width multiplication still produces correct results
</success_criteria>

<output>
After completion, create `.planning/phases/87-scope-segfault-fixes/87-01-SUMMARY.md`
</output>
