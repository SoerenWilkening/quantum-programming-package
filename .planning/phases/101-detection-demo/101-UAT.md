---
status: complete
phase: 101-detection-demo
source: 101-01-SUMMARY.md, 101-02-SUMMARY.md
started: 2026-03-03T12:00:00Z
updated: 2026-03-03T12:06:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Detection test suite passes
expected: Run `pytest tests/python/test_walk_detection.py -v` — all 36 tests pass across 7 groups.
result: pass

### 2. detect() finds marked structure in binary depth=2 tree
expected: Calling detect() on a binary tree with depth=2 returns True (root overlap drops below 3/8 threshold at power=2).
result: pass

### 3. detect() returns False on depth=1 tree (no false positive)
expected: Calling detect() on a binary tree with depth=1 returns False (root overlap stays above 3/8 threshold).
result: pass

### 4. detect() returns False on ternary tree (no false positive)
expected: Calling detect() on a ternary tree returns False (no spurious detection from different branching factor).
result: pass

### 5. SAT detection demo script runs end-to-end
expected: Run `python examples/sat_detection_demo.py` — script completes without error, shows 3 detection cases (no detection, detection, no false positive) plus predicate comparison, with a PASS/FAIL summary at the end.
result: pass

### 6. Predicate walk stays within 17-qubit budget
expected: Depth=1 binary tree with SAT predicate uses ≤17 qubits (verified at 15 qubits). Depth=2 with raw predicate is documented as exceeding budget (27 qubits).
result: pass

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0

## Gaps

[none yet]
