---
phase: 116-walk-integration-demo
plan: 02
type: execute
wave: 2
depends_on: ["116-01"]
files_modified:
  - src/demo.py
  - tests/python/test_demo.py
autonomous: true
requirements:
  - WALK-05

must_haves:
  truths:
    - "Demo generates a valid quantum circuit for KNK depth-2 walk with quantum legality evaluation"
    - "Demo outputs circuit statistics (qubit count, gate count, depth, build time)"
    - "All quantum logic in demo uses standard ql constructs -- no raw gate emission"
    - "Tests verify circuit construction succeeds without simulation"
  artifacts:
    - path: "src/demo.py"
      provides: "KNK depth-2 walk demo showcasing framework expressiveness"
      contains: "walk_step"
    - path: "tests/python/test_demo.py"
      provides: "Circuit-build-only tests for rewritten demo"
      contains: "test_demo"
  key_links:
    - from: "src/demo.py"
      to: "src/chess_walk.py"
      via: "prepare_walk_data, walk_step imports"
      pattern: "from chess_walk import"
    - from: "src/demo.py"
      to: "src/chess_encoding.py"
      via: "encode_position, build_move_table imports"
      pattern: "from chess_encoding import"
---

<objective>
Rewrite demo.py as a KNK depth-2 framework showcase and create circuit-build-only tests.

Purpose: Demonstrate that quantum chess walk algorithms are writable in natural programming style using standard ql constructs. The demo IS the showcase -- code readability and clarity matter as much as correctness.

Output: Rewritten demo.py and test_demo.py.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/116-walk-integration-demo/116-CONTEXT.md
@.planning/phases/116-walk-integration-demo/116-RESEARCH.md
@.planning/phases/116-walk-integration-demo/116-01-SUMMARY.md
@src/demo.py
@src/chess_walk.py
@tests/python/test_demo.py

<interfaces>
<!-- Post Plan 01 interfaces (from chess_walk.py rewrite) -->

From src/chess_walk.py (after Plan 01):
```python
def prepare_walk_data(wk_sq, bk_sq, wn_squares, max_depth) -> list[dict]:
    """Returns list of per-level dicts with keys:
    move_table, move_count, branch_width, apply_move, predicates, entry_qarray_keys"""

def create_height_register(max_depth) -> qint:
def create_branch_registers(max_depth, move_data_per_level) -> list[qint]:
def all_walk_qubits(h_reg, branch_regs, max_depth) -> qint:
def walk_step(h_reg, branch_regs, board_arrs, oracle_per_level, move_data_per_level, max_depth):
```

From src/chess_encoding.py:
```python
def encode_position(wk_sq, bk_sq, wn_squares) -> dict:
    """Returns {'white_king': qarray, 'black_king': qarray, 'white_knights': qarray}"""

def build_move_table(pieces: list[tuple[str, str]]) -> list[tuple[str, int, int]]:
def print_position(wk_sq, bk_sq, wn_squares):
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite demo.py for KNK depth-2 walk</name>
  <files>src/demo.py</files>
  <action>
Replace demo.py entirely with a clean KNK depth-2 walk showcase. Per user decisions:

**Position:** White king e4 (sq 28), white knight c3 (sq 18), black king e8 (sq 60).
**Walk depth:** 2 (white move + black response).
**Output:** Minimal stats -- qubit count, gate count, circuit depth, build time.
**Mode:** Circuit-build-only (no Qiskit simulation).

**Structure:**

1. Module docstring: Explain this is a quantum chess walk demo showing natural programming style for quantum algorithms. Circuit-build-only, no simulation.

2. Imports: `time`, `math`, `quantum_language as ql`, chess_encoding (`encode_position`, `build_move_table`, `print_position`), chess_walk (`prepare_walk_data`, `create_height_register`, `create_branch_registers`, `all_walk_qubits`, `walk_step`).

3. Constants: `WK_SQ = 28`, `BK_SQ = 60`, `WN_SQUARES = [18]`, `MAX_DEPTH = 2`.

4. `main(visualize=False)` function:

   a. Print banner.

   b. **Position section:** Print position info, call `print_position`.

   c. **Move enumeration section:** Show move tables for white and black using `build_move_table`. White: `[('wk','king'), ('wn','knight')]` -> 16 entries. Black: `[('bk','king')]` -> 8 entries. Print entry counts.

   d. **Circuit construction section:**
      - `c = ql.circuit()`
      - `boards = encode_position(WK_SQ, BK_SQ, WN_SQUARES)`
      - `move_data = prepare_walk_data(WK_SQ, BK_SQ, WN_SQUARES, MAX_DEPTH)`
      - `h_reg = create_height_register(MAX_DEPTH)`
      - `branch_regs = create_branch_registers(MAX_DEPTH, move_data)`
      - Print register info (height width, branch widths per level, board qubits).

   e. **Walk compilation section:**
      - Time the walk_step call.
      - `board_arrs = (boards['white_king'], boards['black_king'], boards['white_knights'])`
      - `oracle_per_level = [md['apply_move'] for md in move_data]`
      - `walk_step(h_reg, branch_regs, board_arrs, oracle_per_level, move_data, MAX_DEPTH)`

   f. **Statistics section:**
      - Print qubit count, gate count, depth from `c`.
      - Print gate breakdown from `c.gate_counts`.
      - Print build time.

   g. Optional visualization (same as current).

   h. Return stats dict: `{'qubit_count': ..., 'gate_count': ..., 'depth': ..., 'build_time': ..., 'gate_counts': ...}`.

5. `if __name__ == '__main__':` with argparse for `--visualize`.

**Readability emphasis:** Clear section headers, descriptive print statements, no complex abstractions. The code itself demonstrates natural quantum programming style. Do NOT import `legal_moves` or `print_moves` (those are classical pre-filtering artifacts). Use `build_move_table` to show the all-moves enumeration approach.

**Remove:** All references to old `legal_moves`, `print_moves` from chess_encoding. Remove the chess_walk._walk_compiled_fn reset (no longer exists after Plan 01).
  </action>
  <verify>
    <automated>cd /Users/sorenwilkening/Desktop/Projects/Quantum_Assembly && python -c "
from demo import main
# Just verify import works and function exists
print('demo import ok')
"</automated>
  </verify>
  <done>demo.py is rewritten for KNK depth-2 walk with build_move_table, prepare_walk_data, walk_step. All quantum logic uses standard ql constructs. Returns stats dict with qubit_count, gate_count, depth, build_time.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite test_demo.py with circuit-build-only tests</name>
  <files>tests/python/test_demo.py</files>
  <action>
Remove old test_demo.py tests entirely and write new circuit-build-only tests per user decision.

**New tests:**

1. `test_demo_circuit_builds` (circuit-build-only):
   - Call `demo.main(visualize=False)`.
   - Assert returns a dict with keys: qubit_count, gate_count, depth, build_time.
   - Assert qubit_count > 0, gate_count > 0, depth > 0.
   - Assert build_time >= 0.
   - NO patching of walk_step -- let the real circuit build run.
   - Note: This test may take significant time (10-60s) and memory. If it OOMs, add a skip marker and document.

2. `test_demo_move_tables`:
   - Verify build_move_table produces correct entry counts:
     - White: `build_move_table([('wk','king'), ('wn','knight')])` -> 16 entries
     - Black: `build_move_table([('bk','king')])` -> 8 entries

3. `test_demo_prepare_walk_data_structure`:
   - Call `prepare_walk_data(28, 60, [18], 2)` (no circuit needed -- it's classical precomputation that builds compiled functions).
   - Actually this DOES need a circuit context since it creates @ql.compile functions. Use `clean_circuit` fixture.
   - Assert returns list of 2 dicts (one per level).
   - Assert each dict has keys: move_table, move_count, branch_width, apply_move, predicates, entry_qarray_keys.
   - Assert level 0 (white): move_count == 16, branch_width == 4.
   - Assert level 1 (black): move_count == 8, branch_width == 3.
   - Assert len(predicates) == move_count for each level.

**Fixtures:**
- Use existing `clean_circuit` fixture from conftest.py.
- Remove the `_reset_walk_cache` autouse fixture (no longer needed -- _walk_compiled_fn removed).

**Remove:** test_demo_main (old patched test), test_comparison_main (chess_comparison may not exist or is unrelated).

**Test timeout:** If the full circuit build test is too slow/memory-heavy, mark it with `@pytest.mark.slow` and note in docstring. But try to run it first.
  </action>
  <verify>
    <automated>cd /Users/sorenwilkening/Desktop/Projects/Quantum_Assembly && pytest tests/python/test_demo.py -x -v -k "not circuit_builds" 2>&1 | tail -20</automated>
  </verify>
  <done>test_demo.py has circuit-build-only tests verifying demo construction succeeds, move tables are correct, and prepare_walk_data returns proper structure. Old patched tests removed.</done>
</task>

</tasks>

<verification>
- `pytest tests/python/test_demo.py -x -v -k "not circuit_builds"` passes (fast tests)
- `python -c "from demo import main; print('ok')"` succeeds
- `grep -c "raw" src/demo.py` returns 0 (no raw gate emission)
- `grep -c "get_legal_moves_and_oracle\|legal_moves\|print_moves" src/demo.py` returns 0
- Demo code uses only standard ql constructs (with, operators, @ql.compile, ql.array)
</verification>

<success_criteria>
- demo.py generates a valid circuit for KNK depth-2 walk (WALK-05)
- demo.py uses only standard ql constructs, no raw gate emission (WALK-05)
- demo.py outputs qubit count, gate count, circuit depth, build time (WALK-05)
- test_demo.py verifies circuit construction and data structures without simulation
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/116-walk-integration-demo/116-02-SUMMARY.md`
</output>
